#ifndef UTILMACS_H
#define UTILMACS_H



/* ----- PUBLIC MACROS ----- */

#define MacWrap(expr) do {expr} while (0)

#define Min(a, b) ((a) < (b)) ? (a) : (b)

#define Max(a, b) ((a) > (b)) ? (a) : (b)



#endif /* UTILMACS_H */
